# Plan: prj-8501-task-mugp60ls

**Project:** `PRJ-7219`  
**Task ID:** `task-mugp60ls`  
**Repo:** `prj-8501-task-mugp60ls`  

## Summary

План декомпозиции: создать структуру проекта и базовый модуль конвертера (sub-1), затем CLI-интерфейс (sub-2), и наконец покрыть тестами (sub-3). Каждый этап независимо проверяется.

## Subtasks

### 1. Создать структуру проекта и базовый модуль конвертера

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать репозиторий (package.json, .gitignore, README.md), создать src/converter.js с функцией markdownToHtml(md) возвращающей HTML-строку. Поддержать базовый набор: заголовки h1-h3, жирный/курсив, списки, параграфы, code/inline-code.
- **Test plan:** Проверить: node -e "const c=require('./src/converter'); console.log(c('# Hi'))" возвращает строку содержащую <h1>. Файлы package.json, README.md, .gitignore существуют.

### 2. Реализовать CLI-интерфейс

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить bin/cli.js: чтение Markdown-файла из аргумента или stdin, вызов markdownToHtml, запись результата в stdout или файл из флага --out. Подключить бинарь в package.json (bin).
- **Test plan:** Запуск: node bin/cli.js README.md выводит HTML в stdout. С echo "# t" | node bin/cli.js работает stdin. node bin/cli.js in.md --out out.html создаёт файл.
- **Dependencies:** `sub-1`

### 3. Покрыть тестами и примерами

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить test/test.js с юнит-тестами на конвертер (покрытие базовой разметки и граничные кейсы), обновить README.md примерами использования. Запустить локально node test/test.js — все тесты зелёные.
- **Test plan:** node test/test.js завершается с кодом 0, выводит passed:N (N>=8). README содержит секцию Usage с примерами CLI и require.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
